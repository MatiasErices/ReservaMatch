spring:
  application:
    name: eureka.server

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false ## este proyecto no es un cliente eureka
    fetch-registry: false ## que no se registre a si mismo como cliente eureka
    service-url:
      defaultZone: http://localhost:8761/eureka/
